{
  "name": "@workbench/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:client": "rm -f public/*.js public/*.wasm && esbuild client/ui.tsx client/landing.tsx client/security.ts client/onboarding.ts client/dashboard.tsx --bundle --format=esm --jsx=automatic --jsx-import-source=react --minify --splitting --outdir=public && cp ../../node_modules/@worldcoin/idkit-core/dist/idkit_wasm_bg.wasm public/idkit_wasm_bg.wasm",
    "predev": "npm run build:client",
    "dev": "wrangler dev",
    "predeploy": "npm run build:client",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "typecheck": "wrangler types && tsc --noEmit && tsc --noEmit -p client/tsconfig.json",
    "test": "vitest run",
    "db:migrate:local": "wrangler d1 migrations apply codestation --local",
    "db:migrate:remote": "wrangler d1 migrations apply codestation --remote",
    "invite:create": "tsx scripts/generate-invite.ts"
  },
  "dependencies": {
    "@base-ui/react": "^1.6.0",
    "@workbench/contract": "*",
    "@simplewebauthn/browser": "^13.3.0",
    "@simplewebauthn/server": "^13.3.2",
    "@worldcoin/idkit": "^4.2.1",
    "hono": "^4.7.4",
    "motion": "^12.42.2",
    "react": "^19.2.7",
    "react-dom": "^19.2.7"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "@types/react": "^19.2.14",
    "@types/react-dom": "^19.2.3",
    "esbuild": "^0.28.1",
    "tsx": "^4.23.1",
    "typescript": "^5.7.3",
    "vitest": "^3.0.5",
    "wrangler": "^4.4.0"
  }
}
